import { MockNotice } from "@/components/mock-notice";
export default function History() { return <main className="shell max-w-4xl py-10"><p className="eyebrow">Análise</p><h1 className="mt-2 text-3xl font-bold">Histórico de preços</h1><div className="mt-7"><MockNotice /></div><section className="card"><h2 className="font-bold">FOR → LIS</h2><div className="mt-8 flex h-44 items-end justify-around gap-4 border-b border-slate-200 px-5">{[1240, 1160, 990, 1100, 850].map((price, i) => <div key={i} className="flex flex-1 flex-col items-center gap-2"><div className="w-full rounded-t bg-blue-500" style={{ height: `${price / 8}px` }} /><span className="text-xs text-slate-500">R$ {price}</span></div>)}</div><p className="mt-5 text-sm text-slate-500">Gráfico ilustrativo de dados MOCK. O histórico real será gravado quando um provedor autorizado for integrado.</p></section></main>; }
