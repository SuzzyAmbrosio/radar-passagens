# Required: PostgreSQL connection string. Never commit your real credentials.
DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/radar_passagens?schema=public"
# Required when authentication is implemented. Keep server-side only.
AUTH_SECRET="replace-with-a-long-random-secret"
